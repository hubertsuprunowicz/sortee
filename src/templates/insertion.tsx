import React, {
} from "react"
import styled from "styled-components"

const Wrapper = styled.div`
  display: flex;
  position: relative;
  flex-direction: column;
  align-items: center;
  height: 100%;
`

const InsertionSort: React.FC<{ path: string }> = () => {

  return (
    <Wrapper>
        asdasd
    </Wrapper>
  )
}

export default InsertionSort
